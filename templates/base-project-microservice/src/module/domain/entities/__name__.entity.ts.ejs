export class <%= pascalName %> {
  <% fields.forEach(({ name, tsType }) => { -%> 
  <%- name %>?: <%- tsType  %>  
  <% }) -%>

  constructor(props?: Partial<<%= pascalName %>>) {
    Object.assign(this, props);
  }
}